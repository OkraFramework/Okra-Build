@{
    var path = Path.Combine(testDir, "*/project.json");
    
    foreach (var projectFile in Files.Include(path))
    {
        var args = string.Format("test {0}", projectFile);
        Dotnet(args);
    }
}